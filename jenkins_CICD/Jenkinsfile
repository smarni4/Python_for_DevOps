pipeline {
    agent { label slave-1 }
    stages {
        stage('checking the Java version') {
            steps {
		script {
			echo "Running on slave-1 node.."
	                sh 'java --version'
            	}
	    }
        }
    }
}
